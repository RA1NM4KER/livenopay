export const chartMargin = { left: 4, right: 12, top: 8, bottom: 0 };

export const chartTooltipStyle = {
  backgroundColor: "rgb(var(--color-paper))",
  borderColor: "rgb(var(--color-line))",
  borderRadius: 8,
  boxShadow: "var(--shadow-soft)",
  color: "rgb(var(--color-ink))"
};

export const chartColors = {
  accent: "rgb(var(--color-accent))",
  accentSoft: "rgb(var(--color-accent-soft))",
  ink: "rgb(var(--color-ink))",
  line: "rgb(var(--color-line))",
  paper: "rgb(var(--color-paper))",
  projection: "rgb(var(--color-projection))",
  spend: "rgb(var(--color-spend))",
  usage: "rgb(var(--color-usage))"
};
